# Archived band node layout

Explored 2026-07 — not registered in the canvas, kept as a visual reference.

| File            | Idea                                                         |
| --------------- | ------------------------------------------------------------ |
| `BandsTint.tsx` | Soft artifact wash on full-width input and output port bands |

`BandsTint.tsx` is self-contained apart from current production canvas contracts,
styles, and handle utilities. Reviving it no longer depends on `/spikes/nodes` or
the deleted spike primitives: adapt the archived component to the current node
registry deliberately if the band layout is revisited.
